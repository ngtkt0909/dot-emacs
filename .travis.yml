language: generic
sudo: false
addons:
  apt:
    packages:
      - emacs24
script:
  - emacs --version
  - emacs -batch --eval
    "(progn (setq byte-compile-error-on-warn t) (batch-byte-compile))"
    .emacs.el
